%nav.navbar.navbar-default{:role => "navigation"}
  .container-fluid
    .navbar-header
      %button.navbar-toggle{"data-target" => "#bs-example-navbar-collapse-3", "data-toggle" => "collapse", :type => "button"}
        %span.sr-only Toggle navigation
        %span.icon-bar
        %span.icon-bar
        %span.icon-bar
      %a.navbar-brand{:href => "/"} Brand
    #bs-example-navbar-collapse-3.collapse.navbar-collapse.navbar-right
      - if user_signed_in?
        - if current_page?(root_path)
          = link_to( "Dashboard", apps_path(current_user), class: "btn btn-default navbar-btn" )
        -else
          = link_to( "Log out", destroy_user_session_path, method: "DELETE",class: "btn btn-default navbar-btn" )
      -else
        = link_to( "Log in", new_user_session_path, class: "btn btn-default navbar-btn" )
        = link_to( "Registration", new_user_registration_path, class: "btn btn-default navbar-btn" )